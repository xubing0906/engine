layout(location = 0) out vec4 fragColorX;
#include <common/color/gamma>
#include <common/color/aces>
#if CC_PIPELINE_TYPE == CC_PIPELINE_TYPE_DEFERRED && !CC_FORCE_FORWARD_SHADING
  void main()  { fragColorX = vec4(0.0, 1.0, 0.0, 1.0); }
#else
  void main()  {
    vec3 color = SRGBToLinear(texture(rgbeTex, DEFAULT_UV).rgb);
    fragColorX = packRGBE(color);
  }
#endif
